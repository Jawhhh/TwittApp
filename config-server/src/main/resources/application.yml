server:
  port: 8888
  address: 0.0.0.0

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Jawhhh/config.git
          refresh-rate: 5
          default-label: main
          search-paths: "{application}"