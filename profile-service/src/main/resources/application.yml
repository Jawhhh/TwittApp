server:
  port: 8082
  address: 0.0.0.0

spring:
  profiles:
    default: prod
  application:
    name: profile-service
  config:
    import: 'optional:configserver:http://localhost:8888'
  cloud:
    config:
      fail-fast: true

