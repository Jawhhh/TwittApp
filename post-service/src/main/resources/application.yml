server:
  port: 8085
  address: 0.0.0.0

spring:
  profiles:
    default: prod
  application:
    name: post-service
  config:
    import: 'configserver:http://config:8888'
  cloud:
    config:
      fail-fast: true
